python-dotenv>=1.0
anthropic>=0.40
msal>=1.31
requests>=2.32
APScheduler>=3.10
python-dateutil>=2.9
python-telegram-bot>=21.0
playwright>=1.45
fastapi>=0.115
uvicorn[standard]>=0.32
# Bundles IANA tz data so APScheduler's CronTrigger(timezone="America/Los_Angeles")
# works on slim Python images (the Playwright base image doesn't ship tzdata).
tzdata>=2024.1
plaid-python>=24.0
aioapns>=4.0
# Apple Sign-In: validate Apple's identity-token JWTs (RS256) and sign our
# own session JWTs (HS256). [crypto] pulls in `cryptography` for RS256 keys.
pyjwt[crypto]>=2.10
# Gmail integration (peer to Outlook). google-auth handles token refresh,
# google-auth-oauthlib gives us the Flow class for the magic-link auth-code
# flow, google-api-python-client wraps the Gmail REST API.
google-auth>=2.30
google-auth-oauthlib>=1.2
google-api-python-client>=2.140
